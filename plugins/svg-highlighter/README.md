# @svg-shards/highlighter

Highlight SVG shards one at a time with optional zoom/pan/rotate viewport.

Depends on [svg-shards](../../).

## Demo

```bash
npm run demo
```

Opens at [http://localhost:3000/](http://localhost:3000/) — builds libs, copies them into `demo/vendor/`, then serves the demo folder.
